# python-utils

Simple open-source web projects and Python utility scripts.

## Contents

- `hello.py`  
  Basic example script that prints a greeting using the `greet()` function.

- `calculator.py`  
  Small calculator utility with `add()` and `subtract()` functions.

## How to run

1. Install Python 3 (if not already installed).
2. Clone the repository:

   ```bash
   git clone https://github.com/safeerh2005-dotcom/python-utils.git
   cd python-utils
